This project was bootstrapped with [Create React App](https://github.com/facebookincubator/create-react-app).

## Burger Builder

User can order a burger and can add/remove ingredients

### Installation

The following command will install all the dependencies from package.json file:

```JavaScript
npm install
```

### Running

Run the development server with the command:
```
npm start
```
